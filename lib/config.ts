export const HIDEEX_FUNCTION = true; // If true, hides plugin settings
export const HIDE_BETAFUN = true;    // If true, hides experimental features
export const HIDE_CLOUDSYNC = true;  // If true, hides cloud sync function
