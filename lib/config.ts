export const HIDEEX_FUNCTION = false; // If true, hides plugin settings
export const HIDE_BETAFUN = false;    // If true, hides experimental features
export const HIDE_CLOUDSYNC = false;  // If true, hides cloud sync function
